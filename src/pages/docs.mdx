<h2 id="AWS_ARN">AWS ARN ID</h2>

1.  We need aws arn id to create IAM account using external user. Please follow the [aws_guide](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_common-scenarios_third-party.html) to create an ARN id.
2.  Use the following redhuntlabs aws details

    - AWS Account ID: `647087456535`
    - AWS IAM Policy:

<img
  src={props.images && props.images[0] ? props.images[0] : ""}
  style={{ width: 800 }}
  alt="imageTest"
></img>

<h2 id="subheading1">Subheading 1</h2>

<iframe
  style={{ width: 800, height: 500 }}
  src="https://www.youtube.com/embed/Id64silK_7M"
  frameborder="0"
  allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
  allowfullscreen
></iframe>

<h2 id="subheading2">Subheading 1</h2>

1.  We need aws arn id to create IAM account using external user. Please follow the [aws_guide](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_common-scenarios_third-party.html) to create an ARN id.
2.  Use the following redhuntlabs aws details

    - AWS Account ID: `647087456535`
    - AWS IAM Policy:

    1.  We need aws arn id to create IAM account using external user. Please follow the [aws_guide](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_common-scenarios_third-party.html) to create an ARN id.

3.  Use the following redhuntlabs aws details

    - AWS Account ID: `647087456535`
    - AWS IAM Policy:

    1.  We need aws arn id to create IAM account using external user. Please follow the [aws_guide](https://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_common-scenarios_third-party.html) to create an ARN id.

4.  Use the following redhuntlabs aws details

    - AWS Account ID: `647087456535`
    - AWS IAM Policy:

<h2 id="subheading3">Subheading 1</h2>
<h2 id="subheading4">Subheading 1</h2>
<h2 id="subheading1">Subheading 1</h2>

export default function Layout({ children }) {
  return <>{children}</>
}
